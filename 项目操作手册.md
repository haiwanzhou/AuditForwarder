# AuditForwarder 项目操作手册

本手册指导你在一台**全新的本地开发机**上独立完成 AuditForwarder 的环境准备、依赖安装、配置、构建、运行、验证与排错。按章节顺序操作即可，每一步都给出了可直接复制的命令和预期结果。

- 适用版本：AuditForwarder 1.0.0
- 主要验证环境：Windows 10/11 + MSYS2（GCC 16、OpenSSL 3.6、zlib 1.3、CMake + Ninja）
- 同时给出 Linux（Ubuntu/Debian）和 Visual Studio 2022 的操作方法

---

## 1. 项目速览

AuditForwarder 是跨平台企业安全审计系统，一次构建生成三个可执行程序：

| 可执行程序 | 源码入口 | 角色 |
| --- | --- | --- |
| `auditforwarder-server` | [server_main.cpp](server/src/server_main.cpp) | 监管服务端：管理 API、Web 页面、主机注册/心跳/命令下发 |
| `auditforwarder-client` | [client_main.cpp](client/src/client_main.cpp) | 用户端 Agent：事件采集、心跳、指标与审计摘要上报、命令轮询 |
| `auditforwarderd` | [main.cpp](client/src/main.cpp) | 一体化兼容入口（客户端 + 本机管理后台），可安装为系统服务 |

```text
AuditForwarder/
├── client/      客户端 Agent（采集器、处理器、传输、自保护、系统服务）
├── server/      服务端管理 API 与 Web 前端（server/web/ 为页面静态资源）
├── shared/      客户端/服务端共享代码（配置、日志、加密、证据链、规则引擎、数据库模型）
├── include/     公共头文件（auditforwarder/*.h）
├── config/      配置模板与检测规则
├── scripts/     安装脚本、PostgreSQL 建库/初始化/备份脚本
├── tests/       单元测试
├── docs/        架构、协议、API、数据库等专题文档
└── CMakeLists.txt  统一构建入口
```

数据流：`采集器 → 处理器(脱敏/去重) → SHA-256 哈希链/Merkle 批次存证 → HTTPS 上报服务端 → Web 展示与远程控制`。

---

## 2. 环境要求

| 工具 | 版本要求 | 用途 | 是否必装 |
| --- | --- | --- | --- |
| CMake | ≥ 3.16（建议 3.20+） | 生成构建系统 | 必装 |
| C++17 编译器 | GCC 9+ / Clang 12+ / MSVC 2019+ | 编译源码 | 必装 |
| OpenSSL | ≥ 1.1.1（实测 3.6.3） | 哈希、HMAC、AES-GCM、Ed25519、TLS | 必装 |
| zlib | ≥ 1.2（实测 1.3.2） | 传输压缩 | 必装 |
| Ninja 或 Make | 任意近期版本 | 构建执行器（Ninja 在 Windows 上更快） | 二选一 |
| Node.js / npm | Node ≥ 18、npm ≥ 9 | 仅用于封装 `npm run build` 等便捷命令，**项目零 npm 依赖** | 可选 |
| PostgreSQL | ≥ 15 | 验证关系型数据库脚本；默认运行用 JSON/JSONL 文件存储，不装也能跑 | 可选 |
| libaudit | 任意近期版本 | Linux 审计子系统采集（`apt install libaudit-dev`），缺失时自动降级 | Linux 可选 |

磁盘空间：工具链约 3~5 GB，项目构建产物约 15 MB。

> 路径要求：项目所在目录**不要包含中文、空格或特殊符号**（例如 `C:\AuditForwarder` 或 `D:\dev\AuditForwarder`），可避免部分编译工具识别异常。

---

## 3. 安装工具链（三选一）

### 3.1 Windows + MSYS2（推荐，本手册验证方式）

1. 从 https://www.msys2.org/ 下载安装 MSYS2（默认 `C:\msys64`）。
2. 打开 “MSYS2 MSYS” 终端，更新基础环境（提示关窗就关闭重开再执行第二条）：

   ```bash
   pacman -Syu
   pacman -Su
   ```

3. 安装 UCRT64 工具链（推荐统一使用 UCRT64 环境，不要混用多个环境）：

   ```bash
   pacman -S --needed mingw-w64-ucrt-x86_64-toolchain \
                      mingw-w64-ucrt-x86_64-cmake \
                      mingw-w64-ucrt-x86_64-ninja \
                      mingw-w64-ucrt-x86_64-openssl \
                      mingw-w64-ucrt-x86_64-zlib
   ```

4. 在 PowerShell 中把 UCRT64 的 bin 目录加入 PATH（临时生效，写进系统环境变量可永久生效）：

   ```powershell
   $env:MSYS2_ROOT = "C:\msys64"
   $env:PATH = "$env:MSYS2_ROOT\ucrt64\bin;$env:MSYS2_ROOT\usr\bin;$env:PATH"
   ```

5. 逐项验证，均应输出版本号：

   ```powershell
   g++ --version
   cmake --version
   ninja --version
   openssl version
   ```

> **环境混用警告（常见坑）**：MSYS2 的 `ucrt64` 与 `mingw64` 是两套独立运行时。如果 PATH 中 `mingw64\bin` 排在 `ucrt64\bin` 前面，程序可能加载到另一套 `libstdc++-6.dll`，表现为**单元测试段错误（SegFault）或启动即崩溃**。确保你实际使用的那套环境的 bin 目录在 PATH 最前面。本手册全部命令默认已按上面顺序设置 PATH。

### 3.2 Windows + Visual Studio 2022（备选）

1. 安装 Visual Studio 2022，勾选“使用 C++ 的桌面开发”（含 MSVC、Windows SDK、CMake）。
2. 用 vcpkg 安装依赖：

   ```powershell
   git clone https://github.com/microsoft/vcpkg C:\vcpkg
   C:\vcpkg\bootstrap-vcpkg.bat
   C:\vcpkg\vcpkg install openssl zlib
   ```

3. 在“x64 Native Tools Command Prompt for VS 2022”中执行第 5 章的 CMake 命令（使用 VS 生成器，见 5.1）。

### 3.3 Linux（Ubuntu / Debian）

```bash
sudo apt update
sudo apt install -y build-essential cmake ninja-build pkg-config \
                    libssl-dev zlib1g-dev libaudit-dev
```

验证：`g++ --version && cmake --version && openssl version`。

### 3.4 可选：Node.js（npm 便捷脚本）

项目主体是 C++/CMake，[package.json](package.json) 的 `dependencies` 与 `devDependencies` 均为空，只封装了常用命令。需要时安装 Node.js 18+，在项目根目录执行一次 `npm install`（仅生成/校验 package-lock.json，不会下载大型依赖）。

---

## 4. 配置项目

### 4.1 配置文件总览（config/）

| 文件 | 用途 |
| --- | --- |
| `server_windows.yaml` | 本地/Windows 服务端配置（监听、Token、登录账号、注册密钥） |
| `client_windows.yaml` | 本地/Windows 客户端配置（服务端地址、主机 ID、心跳间隔） |
| `agent_windows.yaml` | 一体化 `auditforwarderd` 的 Windows 测试配置 |
| `agent.yaml` | Linux 生产主配置模板（安装脚本会复制到 `/etc/auditforwarder/`） |
| `rules.yaml` | 违规检测规则（10 条内置规则，文件/进程/网络等） |
| `database_postgresql.yaml` | PostgreSQL 连接与备份配置示例（可选） |

配置由项目内置的精简 YAML/JSON 解析器读取，**不强制安装 yaml-cpp**；YAML 缩进必须用空格，不能用 Tab。

### 4.2 服务端关键配置（config/server_windows.yaml）

```yaml
manager:
  enabled: true
  listen: 0.0.0.0:8443          # 仅本机测试可改 127.0.0.1:8443
  auth_token: "test-token-12345"          # API Bearer Token，客户端必须一致
  login_username: "admin"
  login_password_sha256: "240be518...720a9"   # 密码 admin123 的 SHA-256
  enrollment_key: "specific_key"          # 主机注册密钥，客户端必须一致
  max_host_count: 1000
  use_tls: false                          # 生产环境改 true 并配置证书
```

### 4.3 客户端关键配置（config/client_windows.yaml）

```yaml
remote:
  enabled: true
  servers:
    - "http://127.0.0.1:8443"     # 服务端地址；跨机改成 http://<服务端IP>:8443
  host_id: "test-agent-001"
  enrollment_key: "specific_key"  # 必须与服务端一致
  heartbeat_interval_sec: 5
  allowed_commands:               # 允许服务端下发的命令白名单
    - collect_status
    - echo
```

> **两端必须一致的两项**：`auth_token` 与 `enrollment_key`。不一致会导致注册失败、命令拉取 401/拒绝。

### 4.4 修改 Web 登录密码

配置中存放的是密码的 SHA-256（不是明文）。在 PowerShell 中计算新密码哈希：

```powershell
$password = "你的新密码"
$sha = [System.Security.Cryptography.SHA256]::Create()
($sha.ComputeHash([System.Text.Encoding]::UTF8.GetBytes($password)) |
  ForEach-Object { $_.ToString("x2") }) -join ""
```

把输出的 64 位十六进制字符串填入服务端配置的 `login_password_sha256`，按需修改 `login_username`，重启服务端生效。

### 4.5 局域网访问

服务端保持 `listen: 0.0.0.0:8443`，并在 Windows 防火墙放行入站端口（管理员 PowerShell）：

```powershell
New-NetFirewallRule -DisplayName "AuditForwarder 8443" -Direction Inbound `
  -Protocol TCP -LocalPort 8443 -Action Allow
```

客户端把 `remote.servers` 改为 `http://<服务端IP>:8443`。

### 4.6 生产上线安全清单

- 修改默认登录账号/密码、`auth_token`、`enrollment_key`；
- 启用 TLS（`use_tls: true` 并配置证书，或前置反向代理终结 TLS）；
- 数据库密码通过环境变量/密钥系统注入，不要写入仓库；
- 按最小权限收敛客户端 `allowed_commands` 白名单。

---

## 5. 构建项目

### 5.1 配置与编译

**MSYS2 / Ninja / Linux（在项目根目录执行）：**

```powershell
cmake -S . -B build -G Ninja -DBUILD_TESTING=ON
cmake --build build
```

**Visual Studio 2022 生成器：**

```powershell
cmake -S . -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_TOOLCHAIN_FILE=C:\vcpkg\scripts\buildsystems\vcpkg.cmake
cmake --build build --config Release
```

成功后产物位置：

| 构建方式 | 产物目录 |
| --- | --- |
| Ninja / Make | `build\`（exe 直接在该目录） |
| Visual Studio 多配置 | `build\Release\` |

应得到 `auditforwarder-server.exe`、`auditforwarder-client.exe`、`auditforwarderd.exe`，开启测试时还有 `auditforwarder_tests.exe`（Linux 下无 `.exe` 后缀）。

编译期警告（如个别 `-Wunused-function`、`#pragma comment` 提示）不影响使用。

### 5.2 npm 等价命令（可选）

| 作用 | npm 脚本 | 等价命令 |
| --- | --- | --- |
| 配置 | `npm run configure` | `cmake -S . -B build -DBUILD_TESTING=ON` |
| 构建 | `npm run build` | `cmake --build build --config Release` |
| 启动服务端 | `npm run start:server` | `build\auditforwarder-server.exe -c config\server_windows.yaml` |
| 启动客户端 | `npm run start:client` | `build\auditforwarder-client.exe -c config\client_windows.yaml` |
| 测试 | `npm test` | `ctest --test-dir build --output-on-failure` |

---

## 6. 本地运行

### 6.1 首次运行前：预建数据目录（重要）

日志组件**不会自动创建日志文件的父目录**。全新仓库首次启动时控制台可能出现一行非致命提示：

```text
logger: failed to open log file: data/server/server.log
```

程序仍会正常运行（日志只输出到控制台），但建议首次运行前先建好目录，之后文件日志即可正常写入：

```powershell
New-Item -ItemType Directory -Force data\server, data\client | Out-Null
```

### 6.2 启动服务端

在**项目根目录**新开终端（工作目录必须是项目根，因为配置里的 `config/...`、`data/...` 以及 Web 资源 `server/web/` 都是相对路径）：

```powershell
# 如未全局配置，先设置 PATH：
$env:PATH = "C:\msys64\ucrt64\bin;C:\msys64\usr\bin;$env:PATH"

.\build\auditforwarder-server.exe -c .\config\server_windows.yaml -L info
```

看到 `auditforwarder-server 1.0.0 正在运行。按 Ctrl+C 停止。` 即启动成功。Linux：

```bash
./build/auditforwarder-server -c config/server_windows.yaml -L info
```

### 6.3 启动客户端

保持服务端运行，**另开一个终端**：

```powershell
$env:PATH = "C:\msys64\ucrt64\bin;C:\msys64\usr\bin;$env:PATH"
.\build\auditforwarder-client.exe -c .\config\client_windows.yaml -L info
```

客户端启动后每 5 秒上报心跳，每 10 秒上报审计摘要。日志在 `data/client/client.log`。

### 6.4 一体化模式（可选）

```powershell
.\build\auditforwarderd.exe -c .\config\agent_windows.yaml -L info
```

### 6.5 访问 Web 管理页面

浏览器打开：

```text
http://127.0.0.1:8443/
```

本地测试登录：

```text
账号：admin
密码：admin123
```

登录后可在“主机管理”页看到 `test-agent-001` 在线及 CPU/内存指标。连续 3 次密码错误账号锁定 15 分钟（接口返回 429 `login_locked`）。

---

## 7. 安装后验证清单

按顺序执行，全部通过即表示本地环境完整可用。

**① 可执行文件帮助正常（同时验证运行时 DLL 齐全）**

```powershell
.\build\auditforwarder-server.exe --help
.\build\auditforwarder-client.exe --help
```

若弹出“找不到 libcrypto-3-x64.dll / libstdc++-6.dll / zlib1.dll”，见第 9 章排错。

**② 单元测试全部通过（56 项断言）**

```powershell
ctest --test-dir build --output-on-failure
```

预期：`100% tests passed`。

**③ 无 Token 访问被拒（预期 401）**

```powershell
try { Invoke-WebRequest -Uri "http://127.0.0.1:8443/status" -UseBasicParsing }
catch { "HTTP $($_.Exception.Response.StatusCode.value__)" }   # 期望 401
```

**④ 带 Token 查询状态**

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8443/status" `
  -Headers @{ Authorization = "Bearer test-token-12345" }
```

预期包含 `"running": true`。curl 等价写法：

```bash
curl -H "Authorization: Bearer test-token-12345" http://127.0.0.1:8443/status
```

**⑤ 登录接口签发会话 Token**

```powershell
$body = @{ username="admin";
  password_sha256="240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9" } |
  ConvertTo-Json
Invoke-RestMethod -Uri "http://127.0.0.1:8443/auth/login" -Method Post `
  -Body $body -ContentType "application/json"
```

预期返回 `token`、`token_type: Bearer`、`expires_in: 28800`。

**⑥ 客户端上线**

客户端运行约 10 秒后：

```powershell
(Invoke-RestMethod -Uri "http://127.0.0.1:8443/hosts" `
  -Headers @{ Authorization = "Bearer test-token-12345" }).hosts
```

预期主机 `test-agent-001` 的 `online` 为 `true`，`last_seen` 为当前时间。Web 页面“主机管理”中同步可见。

**⑦ 运行数据已生成**

`data/server/` 下出现 `hosts.json`、`host_metrics/`、`audit_summaries/` 等；`data/server/server.log` 与 `data/client/client.log` 有持续日志。

---

## 8. 系统服务安装（可选，生产部署）

### 8.1 Windows

以**管理员身份**打开 PowerShell：

```powershell
# Ninja 构建产物在 build\ 根目录，需显式指定 SourceDir：
powershell -ExecutionPolicy Bypass -File scripts\install_windows.ps1 `
  -SourceDir .\build -Start

# Visual Studio 构建（产物在 build\Release）可省略 -SourceDir：
powershell -ExecutionPolicy Bypass -File scripts\install_windows.ps1 -Start
```

脚本会把程序安装到 `C:\Program Files\AuditForwarder`，配置/规则放到 `C:\ProgramData\AuditForwarder`，生成 Ed25519 签名密钥，并创建自动启动的 Windows 服务 `AuditForwarder`。

常用维护命令：`Get-Service AuditForwarder`、`Start-Service AuditForwarder`、`Stop-Service AuditForwarder`。卸载：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\install_windows.ps1 -Uninstall
```

### 8.2 Linux

```bash
sudo ./scripts/install_linux.sh --start      # 安装并启动 systemd 服务
sudo systemctl status auditforwarder
sudo ./scripts/install_linux.sh --uninstall  # 卸载
```

---

## 9. 故障排查（Troubleshooting）

| 症状 | 原因 | 解决 |
| --- | --- | --- |
| `cmake` / `g++` 不是内部或外部命令 | 工具链未装或未加入 PATH | 设置第 3.1 节的 PATH 后重开终端；Linux 执行 `sudo apt install build-essential cmake` |
| 启动程序报找不到 `libcrypto-3-x64.dll`、`libstdc++-6.dll`、`zlib1.dll` | 运行时找不到 MSYS2 动态库 | 在运行程序的终端把 `C:\msys64\ucrt64\bin` 加入 PATH（与编译环境一致） |
| ctest 报 `***Exception: SegFault` 或程序闪退 | 混用 ucrt64/mingw64 运行时，加载了错误的 libstdc++ | 让实际工具链的 bin 目录在 PATH **最前**，删除 build 目录后重新构建 |
| 首行 `logger: failed to open log file: ...` | 日志父目录不存在（日志组件不自动建目录） | 预先 `New-Item -ItemType Directory -Force data\server,data\client`；该提示本身非致命 |
| `/status` 等接口返回 `{"error":"unauthorized"}`（401） | 管理 API 需要认证 | 请求头加 `Authorization: Bearer test-token-12345`，或先调 `/auth/login` 拿会话 Token |
| 端口 8443 启动失败/被占用 | 其他进程占用 | `Get-NetTCPConnection -LocalPort 8443` 查占用进程并结束，或修改 `manager.listen` |
| 客户端一直不上线 | 地址不通、Token/密钥不一致、防火墙拦截 | 核对 `remote.servers`、两端 `auth_token`/`enrollment_key` 一致、防火墙已放行；查看 `data/client/client.log` 的报错 |
| `etw: failed to subscribe to Security log` / EvtSubscribe 失败 | 订阅 Windows 安全日志需管理员权限 | 以管理员身份运行终端；仅测试 Web/API 时可忽略，不影响其他采集器 |
| 浏览器打开页面出现 `ui asset not found`（404） | 启动时工作目录不是项目根，找不到 `server/web/` | 先 `cd` 到项目根目录再启动服务端 |
| 登录返回 429 `login_locked` | 连续 3 次密码错误 | 等待 15 分钟自动解锁，或重启服务端 |
| PostgreSQL 初始化失败 | 服务未启动/psql 不在 PATH/顺序错误 | 确认服务运行、先执行 `001_schema.sql` 再执行 `002_seed.sql`、用户名密码与配置一致 |
| 控制台中文显示乱码 | Windows 控制台代码页问题 | VS Code 终端以 UTF-8 保存配置文件；执行 `chcp 65001` 后再启动程序 |
| CMake 找不到 OpenSSL/zlib | 依赖未安装或前缀未指定 | MSYS2 安装对应包；VS 生成器加 `-DCMAKE_TOOLCHAIN_FILE=...vcpkg.cmake` |

---

## 10. 常用命令速查

```powershell
# —— 环境（每个新终端）——
$env:PATH = "C:\msys64\ucrt64\bin;C:\msys64\usr\bin;$env:PATH"

# —— 构建 ——
cmake -S . -B build -G Ninja -DBUILD_TESTING=ON
cmake --build build

# —— 测试 ——
ctest --test-dir build --output-on-failure

# —— 运行（两个终端）——
.\build\auditforwarder-server.exe -c .\config\server_windows.yaml -L info
.\build\auditforwarder-client.exe -c .\config\client_windows.yaml -L info

# —— 清理后从零重建 ——
Remove-Item -Recurse -Force build
cmake -S . -B build -G Ninja -DBUILD_TESTING=ON; cmake --build build
```

---

## 11. PostgreSQL（可选）

默认运行链路使用 JSON/JSONL 文件存储，无需数据库。需要验证 PostgreSQL 方案时：

1. 安装 PostgreSQL 15+，创建数据库与用户：

   ```sql
   CREATE USER auditforwarder WITH PASSWORD '强密码';
   CREATE DATABASE auditforwarder OWNER auditforwarder;
   ```

2. 按顺序执行脚本（顺序不能颠倒）：

   ```powershell
   psql -U auditforwarder -d auditforwarder -f scripts\database\postgresql\001_schema.sql
   psql -U auditforwarder -d auditforwarder -f scripts\database\postgresql\002_seed.sql
   ```

3. 复制 [database_postgresql.yaml](config/database_postgresql.yaml) 为本地配置并按实际修改，注意当前配置键名为 `driver`、`user`：

   ```yaml
   database:
     enabled: true
     driver: postgresql
     host: 127.0.0.1
     port: 5432
     name: auditforwarder
     user: auditforwarder
     password: "通过环境变量注入，不要提交真实密码"
   ```

4. 备份/恢复使用 [backup_restore.ps1](scripts/database/postgresql/backup_restore.ps1)，表结构说明见 [docs/DATABASE_DESIGN.md](docs/DATABASE_DESIGN.md)，接口与参数化查询示例见 [docs/DATABASE_API.md](docs/DATABASE_API.md)。

---

## 12. VS Code 集成（可选）

推荐插件：C/C++（`ms-vscode.cpptools`）、CMake Tools（`ms-vscode.cmake-tools`）、CMake（`twxs.cmake`）、YAML（`redhat.vscode-yaml`）、PowerShell（`ms-vscode.powershell`）。

如需“运行生成任务/调试”，可在项目根创建 `.vscode/tasks.json` 与 `.vscode/launch.json`（`.vscode/` 已被 git 忽略，属于个人配置）：

`.vscode/tasks.json`：

```json
{
  "version": "2.0.0",
  "tasks": [
    { "label": "配置 CMake", "type": "shell", "command": "cmake",
      "args": ["-S", ".", "-B", "build", "-G", "Ninja", "-DBUILD_TESTING=ON"],
      "group": "build", "problemMatcher": [] },
    { "label": "编译项目", "type": "shell", "command": "cmake",
      "args": ["--build", "build"],
      "group": { "kind": "build", "isDefault": true }, "problemMatcher": ["$gcc"] },
    { "label": "运行测试", "type": "shell", "command": "ctest",
      "args": ["--test-dir", "build", "--output-on-failure"],
      "dependsOn": "编译项目", "group": "test", "problemMatcher": [] }
  ]
}
```

`.vscode/launch.json`（调试前确认终端已设置好 MSYS2 PATH；Ninja 产物在 `build/` 下）：

```json
{
  "version": "0.2.0",
  "configurations": [
    { "name": "调试服务端", "type": "cppdbg", "request": "launch",
      "program": "${workspaceFolder}/build/auditforwarder-server.exe",
      "args": ["-c", "config/server_windows.yaml", "-L", "debug"],
      "cwd": "${workspaceFolder}", "externalConsole": false,
      "MIMode": "gdb", "miDebuggerPath": "C:/msys64/usr/bin/gdb.exe",
      "preLaunchTask": "编译项目" },
    { "name": "调试客户端", "type": "cppdbg", "request": "launch",
      "program": "${workspaceFolder}/build/auditforwarder-client.exe",
      "args": ["-c", "config/client_windows.yaml", "-L", "debug"],
      "cwd": "${workspaceFolder}", "externalConsole": false,
      "MIMode": "gdb", "miDebuggerPath": "C:/msys64/usr/bin/gdb.exe",
      "preLaunchTask": "编译项目" }
  ]
}
```

---

## 13. 深入文档地图

| 文档 | 内容 |
| --- | --- |
| [README.md](README.md) | 项目简介与快速开始 |
| [docs/PROJECT_SPEC.md](docs/PROJECT_SPEC.md) | 项目规格与需求说明 |
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | 组件、线程模型、事件生命周期、威胁模型 |
| [docs/PROTOCOL.md](docs/PROTOCOL.md) | 通信协议 |
| [docs/API_REFERENCE.md](docs/API_REFERENCE.md) | HTTP 管理 API 明细 |
| [docs/DATABASE_DESIGN.md](docs/DATABASE_DESIGN.md) / [docs/DATABASE_API.md](docs/DATABASE_API.md) | PostgreSQL 表结构设计 / 数据访问 API 示例 |
| [docs/DEVELOPMENT_GUIDE.md](docs/DEVELOPMENT_GUIDE.md) | 开发约束与新增功能流程 |
| [docs/TEST_PLAN.md](docs/TEST_PLAN.md) | 测试计划 |
| [docs/USER_MANUAL.md](docs/USER_MANUAL.md) | 完整用户手册 |
| [docs/OPERATIONS.md](docs/OPERATIONS.md) | 生产运维（服务生命周期、密钥轮换、升级） |
| [docs/PROJECT_STRUCTURE.md](docs/PROJECT_STRUCTURE.md) | 代码目录边界说明 |

---

## 附录：默认本地参数（仅限测试，生产必须更换）

| 项 | 值 |
| --- | --- |
| Web / API 端口 | 8443（HTTP，未启用 TLS） |
| Web 登录 | admin / admin123 |
| API Bearer Token | test-token-12345 |
| 主机注册密钥 | specific_key |
| 测试主机 ID | test-agent-001 |
