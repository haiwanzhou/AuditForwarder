# AuditForwarder - Windows 服务端测试配置

agent:
  id: "auditforwarder-server"
  data_dir: data/server
  config_path: config/server_windows.yaml

log:
  level: debug
  file: data/server/server.log
  max_bytes: 52428800
  max_backups: 5

chain:
  batch_size: 10
  sign_batches: false
  auto_persist: true
  hmac_key: "test-hmac-key-12345"   # 客户端 chain.hmac_key 必须与此一致，服务端据此验签

transport:
  servers: []
  auth_token: "test-token-12345"
  verify_tls: false

remote:
  enabled: false
  servers: []

detector:
  rules_path: config/rules.yaml
  enable_behavior_baseline: false

self_protect:
  enabled: false

manager:
  enabled: true
  listen: 0.0.0.0:8443
  auth_token: "test-token-12345"
  login_username: "admin"
  login_password_sha256: "240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9"
  enrollment_key: "specific_key"
  max_host_count: 1000
  status_timeout_seconds: 30       # 心跳超过此值判定下线（客户端默认 5s 心跳，留 6 次窗口）
  use_tls: false
  require_client_cert: false
  tls_crl_check: false

collectors:
  enabled: false

processors:
  - type: enricher
