# AuditForwarder 本地测试环境配置说明

本文档说明接收方在本地电脑运行 AuditForwarder 时，需要关注和可能修改的配置位置。所有路径均以项目根目录为基准。

## 1. 配置文件总览

| 配置目标 | 文件路径 | 关键位置 |
| --- | --- | --- |
| 服务端监听地址、Token、登录账号 | `config/server_windows.yaml` | `manager` 段，约第 36-42 行 |
| 服务端数据目录和日志文件 | `config/server_windows.yaml` | `agent`、`log` 段，约第 3-10 行 |
| 客户端连接服务端地址 | `config/client_windows.yaml` | `remote.servers`，约第 29-32 行 |
| 客户端主机 ID 和注册密钥 | `config/client_windows.yaml` | `remote.host_id`、`remote.enrollment_key`，约第 33-40 行 |
| 集成版 Agent 配置 | `config/agent_windows.yaml` | `remote`、`manager` 段，约第 44-82 行 |
| PostgreSQL 连接配置 | `config/database_postgresql.yaml` | `database` 段，约第 4-11 行 |
| npm/CMake 运行命令 | `package.json` | `scripts` 段 |

## 2. 数据库连接配置

文件：

```text
config/database_postgresql.yaml
```

当前配置位置：

```yaml
database:
  enabled: true
  type: postgresql
  host: 127.0.0.1
  port: 5432
  name: auditforwarder
  username: auditforwarder
  password: "请替换为强密码，禁止提交真实生产密码"
```

本地测试修改示例：

```diff
 database:
   enabled: true
   type: postgresql
-  host: 127.0.0.1
+  host: 127.0.0.1
   port: 5432
-  name: auditforwarder
-  username: auditforwarder
-  password: "请替换为强密码，禁止提交真实生产密码"
+  name: auditforwarder_local
+  username: auditforwarder_local
+  password: "<你的本地数据库密码>"
```

说明：

- 当前服务端主流程仍可使用 JSON/JSONL 文件运行，不强制连接 PostgreSQL。
- 如需验证数据库脚本，请先安装 PostgreSQL 15+。
- 不要把真实生产密码写入仓库；生产环境应使用环境变量或密钥管理系统。

数据库初始化命令：

```powershell
psql -U <数据库用户> -d <数据库名> -f scripts\database\postgresql\001_schema.sql
psql -U <数据库用户> -d <数据库名> -f scripts\database\postgresql\002_seed.sql
```

## 3. 服务端 API 地址配置

文件：

```text
config/server_windows.yaml
```

当前服务端监听配置位置：`manager` 段，约第 36-38 行。

当前配置：

```yaml
manager:
  enabled: true
  listen: 0.0.0.0:8443
```

仅本机测试建议改为：

```diff
 manager:
   enabled: true
-  listen: 0.0.0.0:8443
+  listen: 127.0.0.1:8443
```

需要局域网访问时保留：

```yaml
listen: 0.0.0.0:8443
```

同时需要打开 Windows 防火墙入站端口：

```powershell
New-NetFirewallRule -DisplayName "AuditForwarder 8443" -Direction Inbound -Protocol TCP -LocalPort 8443 -Action Allow
```

## 4. 客户端连接服务端地址

文件：

```text
config/client_windows.yaml
```

当前配置位置：`remote.servers`，约第 29-32 行。

本机测试：

```yaml
remote:
  enabled: true
  servers:
    - "http://127.0.0.1:8443"
```

连接另一台服务端时修改为：

```diff
 remote:
   enabled: true
   servers:
-    - "http://127.0.0.1:8443"
+    - "http://<服务端IP>:8443"
```

例如服务端 IP 是 `192.168.1.20`：

```yaml
servers:
  - "http://192.168.1.20:8443"
```

## 5. Token 和注册密钥配置

服务端文件：

```text
config/server_windows.yaml
```

客户端文件：

```text
config/client_windows.yaml
```

交付包默认值（固定凭据已清空）：

```yaml
auth_token: ""
enrollment_key: ""
```

说明：两者为空时，本地测试接口免固定 Token 认证、注册不校验密钥。如需启用，请自行生成高强度值并保持服务端与客户端一致，例如：

```diff
-auth_token: ""
-enrollment_key: ""
+auth_token: "<自行生成的高强度Token>"
+enrollment_key: "<自行生成的注册密钥>"
```

注意：

- 服务端和客户端的 `auth_token` 必须一致。
- 服务端和客户端的 `enrollment_key` 必须一致。
- 如果不一致，客户端注册或命令拉取会失败。
- 生产环境不要使用示例 Token 和示例注册密钥。

## 6. 登录账号和密码哈希

文件：

```text
config/server_windows.yaml
```

当前配置位置：`manager` 段，约第 36-44 行。

交付包当前配置（占位值，无法直接登录）：

```yaml
login_username: "admin"
login_password_sha256: "请替换为强密码的SHA-256哈希"
```

注意：交付包不提供任何默认密码。`login_password_sha256` 为占位值，登录会一直失败，必须按下方步骤生成并替换为自己的密码哈希后才能登录管理后台；切勿将该字段留空（程序会回退到内置默认密码）。

修改步骤：

1. 在 PowerShell 中计算新密码 SHA-256：

```powershell
$password = "你的新密码"
$sha = [System.Security.Cryptography.SHA256]::Create()
$bytes = [System.Text.Encoding]::UTF8.GetBytes($password)
($sha.ComputeHash($bytes) | ForEach-Object { $_.ToString("x2") }) -join ""
```

2. 修改配置：

```diff
-login_username: "admin"
-login_password_sha256: "请替换为强密码的SHA-256哈希"
+login_username: "local_admin"
+login_password_sha256: "<新密码的SHA-256哈希>"
```

## 7. 环境变量设置

本项目默认不要求 `.env` 文件。

Windows + MSYS2 本地测试建议在 VS Code 终端设置：

```powershell
$env:MSYS2_ROOT = "<MSYS2安装目录>"
$env:PATH = "$env:MSYS2_ROOT\mingw64\bin;$env:MSYS2_ROOT\usr\bin;$env:PATH"
```

如果后续接入真实 PostgreSQL，可选择使用环境变量保存敏感信息：

```powershell
$env:AF_DB_HOST = "127.0.0.1"
$env:AF_DB_PORT = "5432"
$env:AF_DB_NAME = "auditforwarder_local"
$env:AF_DB_USER = "auditforwarder_local"
$env:AF_DB_PASSWORD = "<你的本地数据库密码>"
```

说明：当前配置解析主要读取 YAML 文件，上述数据库环境变量是推荐的安全扩展方式；生产接入数据库驱动时应优先使用环境变量。

## 8. 本地测试专用配置项

### 8.1 关闭 TLS

本地测试默认：

```yaml
use_tls: false
require_client_cert: false
```

生产环境建议：

```diff
-use_tls: false
-require_client_cert: false
+use_tls: true
+require_client_cert: true
```

同时配置证书路径。

### 8.2 客户端允许的远程命令

文件：

```text
config/client_windows.yaml
```

当前本地测试配置：

```yaml
allowed_commands:
  - collect_status
  - echo
```

如需限制功能：

```diff
 allowed_commands:
   - collect_status
-  - echo
```

### 8.3 Windows 安全日志采集

文件：

```text
config/agent_windows.yaml
```

当前本地测试中部分采集器可关闭，以避免管理员权限问题：

```yaml
collectors:
  enabled: true
  audit:
    enabled: false
```

如果需要测试 Windows 安全日志采集：

```diff
 collectors:
   enabled: true
   audit:
-    enabled: false
+    enabled: true
```

然后以管理员身份运行 VS Code 或终端。

## 9. 配置修改后验证

构建：

```powershell
npm run configure
npm run build
```

启动服务端：

```powershell
npm run start:server
```

验证状态（`auth_token` 为空时无需认证头；已配置时替换为你的 Token）：

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8443/status" -Headers @{ Authorization = "Bearer <你的auth_token>" }
```

启动客户端：

```powershell
npm run start:client
```

在 Web 页面查看主机是否上线：

```text
http://127.0.0.1:8443/
```

## 10. 交付安全检查

打包前确认：

- `config/*.yaml` 中没有个人电脑绝对路径。
- `config/*.yaml` 中没有真实生产 Token。
- `config/*.yaml` 中没有真实数据库密码。
- 不包含 `.env` 文件。
- 不包含 `node_modules/`。
- 不包含 `build/`、`build_*/`。
- 不包含 `data/` 运行数据。
