# AuditForwarder 项目运行指南

本文档面向项目接收方，目标是在一台新的个人电脑上使用 VSCode 成功构建并运行 AuditForwarder 源码项目。

## 1. 前置环境要求

### 1.1 必装工具

| 工具 | 推荐版本 | 用途 |
| --- | --- | --- |
| VSCode | 最新稳定版 | 打开、编辑、运行项目 |
| CMake | 3.16 或更高 | 生成构建目录 |
| C++ 编译器 | Windows 推荐 MSYS2 MinGW-w64 或 Visual Studio 2022；Linux 推荐 GCC 9+ | 编译源码 |
| OpenSSL | 1.1.1 或更高 | 哈希、签名、加密相关功能 |
| zlib | 1.2 或更高 | 压缩传输支持 |
| Node.js | 18 或更高 | 可选，用于执行 `npm run dev` 等便捷脚本 |
| npm | 9 或更高 | 可选，用于安装 npm 项目入口 |

### 1.2 数据库要求

项目提供 PostgreSQL 数据库设计和脚本，但当前服务端运行链路仍兼容本地 JSON/JSONL 文件存储，因此首次运行不强制安装数据库。

如需验证数据库脚本，请安装：

- PostgreSQL 15 或更高
- `psql` 命令行工具

数据库脚本位置：

```text
scripts/database/postgresql/001_schema.sql
scripts/database/postgresql/002_seed.sql
```

## 2. 获取项目

### 2.1 解压 ZIP 包

本项目交付方式为标准 ZIP 压缩包，不要求接收方使用 `git clone`。

```text
将 AuditForwarder_source.zip 解压到任意本地目录，然后进入解压后的 AuditForwarder 目录。
```

注意：项目路径不要包含中文、空格或特殊符号，避免部分编译工具识别异常。

## 3. 用 VSCode 打开项目

1. 打开 VSCode。
2. 点击“文件 -> 打开文件夹”。
3. 选择 `AuditForwarder` 项目根目录。
4. 打开 VSCode 终端：`终端 -> 新建终端`。
5. 确认终端当前位置是项目根目录。

检查命令：

```powershell
Get-ChildItem
```

正常情况下应看到：

```text
client
config
docs
include
scripts
server
shared
tests
CMakeLists.txt
package.json
README.md
```

## 4. 安装依赖

### 4.1 npm 入口依赖

本项目主体是 C++/CMake，不依赖前端 npm 包。`package.json` 只用于封装 VSCode 终端里的常用命令。

在项目根目录执行：

```powershell
npm install
```

预期结果：

- 生成 `package-lock.json`
- 不会下载大型依赖
- 不会生成庞大的 `node_modules`

### 4.2 C++ 编译依赖

Windows 推荐安装 MSYS2，并安装 MinGW、CMake、OpenSSL、zlib：

```powershell
pacman -S --needed mingw-w64-x86_64-toolchain mingw-w64-x86_64-cmake mingw-w64-x86_64-openssl mingw-w64-x86_64-zlib
```

如果使用 Visual Studio 2022，请安装：

- “使用 C++ 的桌面开发”
- CMake 工具
- Windows SDK

Linux Ubuntu 示例：

```bash
sudo apt update
sudo apt install -y build-essential cmake pkg-config libssl-dev zlib1g-dev libaudit-dev
```

## 5. 环境变量配置

项目默认不需要 `.env` 文件。

如果 Windows 使用 MSYS2，建议在当前 VSCode 终端临时配置：

```powershell
$env:MSYS2_ROOT = "<MSYS2安装目录>"
$env:PATH = "$env:MSYS2_ROOT\mingw64\bin;$env:MSYS2_ROOT\usr\bin;$env:PATH"
```

其中 `<MSYS2安装目录>` 请替换为你的实际安装目录。

检查工具是否可用：

```powershell
cmake --version
g++ --version
openssl version
```

## 6. 数据库初始化

首次运行项目不强制初始化 PostgreSQL。

如需初始化数据库，请先创建数据库和用户，再执行：

```powershell
psql -U <数据库用户> -d <数据库名> -f scripts\database\postgresql\001_schema.sql
psql -U <数据库用户> -d <数据库名> -f scripts\database\postgresql\002_seed.sql
```

数据库配置模板：

```text
config/database_postgresql.yaml
```

安全提醒：

- 不要把真实数据库密码提交到仓库。
- 生产环境请通过环境变量或密钥管理系统注入数据库密码。
- 默认 Token、默认登录账号仅用于本地测试。

## 7. 构建项目

### 7.1 推荐方式：npm 脚本

```powershell
npm run configure
npm run build
```

### 7.2 直接使用 CMake

```powershell
cmake -S . -B build -DBUILD_TESTING=ON
cmake --build build --config Release
```

构建成功后，`build` 目录中应出现：

```text
auditforwarder-server.exe
auditforwarder-client.exe
auditforwarderd.exe
```

Linux/macOS 下可执行文件通常没有 `.exe` 后缀。

## 8. 启动开发环境

### 8.1 一键启动服务端

```powershell
npm run dev
```

该命令会自动配置、构建并启动服务端。

### 8.2 分步启动服务端

```powershell
npm run start:server
```

或：

```powershell
.\build\auditforwarder-server.exe -c .\config\server_windows.yaml
```

启动成功后访问：

```text
http://127.0.0.1:8443/
```

本地测试登录信息：

```text
账号：admin
密码：admin123
```

### 8.3 启动客户端

新开一个 VSCode 终端，执行：

```powershell
npm run start:client
```

或：

```powershell
.\build\auditforwarder-client.exe -c .\config\client_windows.yaml
```

客户端启动后会向服务端发送心跳、资源指标和审计摘要。

## 9. 测试验证

执行单元测试：

```powershell
npm test
```

或：

```powershell
ctest --test-dir build --output-on-failure
```

验证服务端状态：

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8443/status" -Headers @{ Authorization = "Bearer test-token-12345" }
```

预期结果包含：

```text
running: true
```

## 10. 项目目录结构

```text
AuditForwarder/
├── client/                 # 客户端核心、采集器、传输模块
├── config/                 # 本地运行配置和规则配置
├── docs/                   # 架构、API、数据库、测试和运维文档
├── include/auditforwarder/ # 公共头文件
├── scripts/                # 安装脚本、数据库脚本、备份恢复脚本
├── server/                 # 服务端管理 API 和 Web 管理界面
├── shared/                 # 公共实现、加密、数据库模型、工具代码
├── tests/                  # 单元测试
├── CMakeLists.txt          # CMake 构建入口
├── package.json            # VSCode/npm 便捷命令入口
└── README.md               # 项目简介
```

## 11. 常见问题排查

### 11.1 `cmake` 命令不存在

原因：CMake 未安装或未加入 PATH。

解决：

- Windows：安装 CMake 或 MSYS2 CMake，并重启 VSCode。
- Linux：执行 `sudo apt install cmake`。

### 11.2 `g++` 命令不存在

原因：C++ 编译器未安装或未加入 PATH。

解决：

- Windows：安装 MSYS2 MinGW-w64，配置 `MSYS2_ROOT` 和 `PATH`。
- Linux：执行 `sudo apt install build-essential`。

### 11.3 启动后访问 `/status` 返回 `unauthorized`

原因：接口需要 Bearer Token。

解决：

```powershell
Invoke-RestMethod -Uri "http://127.0.0.1:8443/status" -Headers @{ Authorization = "Bearer test-token-12345" }
```

### 11.4 端口 8443 被占用

解决：

```powershell
Get-Process -Id (Get-NetTCPConnection -LocalPort 8443).OwningProcess
```

停止占用进程后重试，或修改 `config/server_windows.yaml` 中的：

```yaml
manager:
  listen: 127.0.0.1:8443
```

### 11.5 Windows 安全日志采集失败

原因：订阅 Windows 安全日志通常需要管理员权限。

解决：

- 用管理员身份运行 VSCode 或终端。
- 仅测试服务端页面时，可以忽略该采集错误。

### 11.6 PostgreSQL 初始化失败

检查：

- PostgreSQL 服务是否启动。
- `psql` 是否在 PATH 中。
- 数据库用户、数据库名、密码是否正确。
- 是否先执行 `001_schema.sql`，再执行 `002_seed.sql`。

## 12. 打包和交付说明

交付源码前应确认：

- 不包含 `node_modules/`。
- 不包含 `build/`、`build_*/` 等构建目录。
- 不包含 `data/` 运行数据目录。
- 不包含 `.env`、真实 Token、真实数据库密码或生产证书私钥。
- `.gitignore` 已排除构建产物、运行数据、环境文件和压缩包。

推荐压缩包命名：

```text
AuditForwarder_source.zip
```

接收方解压后，按照本文档从第 2 节开始操作即可。
